# {{ doc_type_name }} Test Specification

---

**Document Metadata**

| Field | Value |
|-------|-------|
| **Document ID** | {{ doc_type_code }}-TEST-SPEC |
| **Version** | {{ version }} |
| **Generated** | {{ generation_date }} |
| **Status** | {{ status }} |
| **Project** | {{ project_name }} |

---

## 1. Introduction

This document specifies the {{ test_type }} tests for {{ project_name }}. This specification is part of the Design History File (DHF) and provides traceability for regulatory compliance.

### 1.1 Purpose

This document provides a comprehensive list of all {{ test_type }} test cases, including their current status, test procedures, and traceability links to validated requirements.

### 1.2 Scope

This specification covers all {{ doc_type_name }}s defined in the MedHarness system as of {{ generation_date }}.

---

## 2. Test Cases

{% for item in items %}
### {{ loop.index }}. {{ item.id }}: {{ item.title }}

<div class="test-case-section" markdown="1">

**Status**: <span class="status-{{ item.status }}">{{ item.status | upper }}</span>  
**Verification Status**: <span class="verification-{{ item.verification_status or 'not_verified' }}">{{ item.verification_status or 'NOT VERIFIED' | upper }}</span>  
{% if item.reviewer %}**Reviewer**: {{ item.reviewer }}  {% endif %}
{% if item.review_date %}**Review Date**: {{ item.review_date }}  {% endif %}

#### Validates/Verifies

{% if item.links %}
{% for link in item.links %}
- {{ link }}
{% endfor %}
{% else %}
*No linked requirements*
{% endif %}

{% if item.prerequisites %}
#### Prerequisites

{{ item.prerequisites }}
{% endif %}

{% if item.steps %}
#### Test Steps

{{ item.steps }}
{% endif %}

{% if item.expected_result %}
#### Expected Result

{{ item.expected_result }}
{% endif %}

{% if item.verified_by %}
#### Verification

**Verified by**: {{ item.verified_by }}  
**Verified date**: {{ item.verified_date }}
{% endif %}

</div>

{% endfor %}

---

## 3. Summary

### 3.1 Statistics

| Metric | Count |
|--------|-------|
| **Total Test Cases** | {{ items | length }} |
| **Approved** | {{ items | selectattr('status', 'equalto', 'approved') | list | length }} |
| **Draft** | {{ items | selectattr('status', 'equalto', 'draft') | list | length }} |
| **Verified** | {{ items | selectattr('verification_status', 'equalto', 'verified') | list | length }} |
| **Failed** | {{ items | selectattr('verification_status', 'equalto', 'failed') | list | length }} |
| **Not Verified** | {{ items | selectattr('verification_status', 'equalto', 'not_verified') | list | length }} |

### 3.2 Test Coverage

{% set total_tests = items | length %}
{% set verified_tests = items | selectattr('verification_status', 'equalto', 'verified') | list | length %}
{% if total_tests > 0 %}
**Verification Rate**: {{ (verified_tests / total_tests * 100) | round(1) }}% ({{ verified_tests }}/{{ total_tests }})
{% endif %}

### 3.3 Traceability

{% set tests_with_links = items | selectattr('links') | list | length %}
{% if total_tests > 0 %}
**Tests with Requirements**: {{ tests_with_links }}/{{ total_tests }} ({{ (tests_with_links / total_tests * 100) | round(1) }}%)
{% endif %}

---

## 4. Document Control

**Document Owner**: Quality Assurance  
**Last Updated**: {{ generation_date }}  
**Next Review**: TBD

---

*This document was automatically generated by MedHarness.*

