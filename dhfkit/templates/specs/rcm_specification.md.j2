# {{ doc_type_name }} Specification

---

**Document Metadata**

| Field | Value |
|-------|-------|
| **Document ID** | {{ doc_type_code }}-SPEC |
| **Version** | {{ version }} |
| **Generated** | {{ generation_date }} |
| **Status** | {{ status }} |
| **Project** | {{ project_name }} |

---

## 1. Introduction

This document specifies the Risk Control Measures for {{ project_name }}. This specification is part of the Design History File (DHF) and provides comprehensive documentation of risk mitigation strategies for regulatory compliance per ISO 14971.

### 1.1 Purpose

This document provides a comprehensive list of all risk control measures, their implementation status, and traceability to the risks they mitigate and system requirements they implement.

### 1.2 Scope

This specification covers all risk control measures defined in the MedHarness system as of {{ generation_date }}.

---

## 2. Risk Control Measures

{% for item in items %}
### {{ loop.index }}. {{ item.id }}: {{ item.title }}

<div class="rcm-section" markdown="1">

**Status**: <span class="status-{{ item.status }}">{{ item.status | upper }}</span>  
{% if item.control_type %}**Control Type**: {{ item.control_type }}  {% endif %}
{% if item.implementation_status %}**Implementation Status**: {{ item.implementation_status }}  {% endif %}

#### Description

{{ item.content }}

{% if item.mitigates %}
#### Mitigates Risks

{% for risk in item.mitigates %}
- {{ risk }}
{% endfor %}
{% endif %}

{% if item.implements %}
#### Implemented By Requirements

{% for req in item.implements %}
- {{ req }}
{% endfor %}
{% endif %}

#### Control Classification

| Attribute | Value |
|-----------|-------|
| **Control Type** | {{ item.control_type }} |
| **Implementation Status** | {{ item.implementation_status }} |
| **Status** | {{ item.status }} |

</div>

{% endfor %}

---

## 3. Summary

### 3.1 Statistics

| Metric | Count |
|--------|-------|
| **Total RCMs** | {{ items | length }} |
| **Approved** | {{ items | selectattr('status', 'equalto', 'approved') | list | length }} |
| **Draft** | {{ items | selectattr('status', 'equalto', 'draft') | list | length }} |

### 3.2 Control Type Distribution

| Control Type | Count |
|--------------|-------|
| **Inherently Safe Design** | {{ items | selectattr('control_type', 'equalto', 'Inherently Safe Design') | list | length }} |
| **Protective Measure** | {{ items | selectattr('control_type', 'equalto', 'Protective Measure') | list | length }} |
| **Information for Safety** | {{ items | selectattr('control_type', 'equalto', 'Information for Safety') | list | length }} |

### 3.3 Implementation Status

| Status | Count |
|--------|-------|
| **Verified** | {{ items | selectattr('implementation_status', 'equalto', 'Verified') | list | length }} |
| **Implemented** | {{ items | selectattr('implementation_status', 'equalto', 'Implemented') | list | length }} |
| **Planned** | {{ items | selectattr('implementation_status', 'equalto', 'Planned') | list | length }} |

---

## 4. Document Control

**Document Owner**: Risk Management  
**Last Updated**: {{ generation_date }}  
**Next Review**: TBD

---

*This document was automatically generated by MedHarness in accordance with ISO 14971.*
