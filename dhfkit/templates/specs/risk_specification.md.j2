# {{ doc_type_name }} Specification

---

**Document Metadata**

| Field | Value |
|-------|-------|
| **Document ID** | {{ doc_type_code }}-SPEC |
| **Version** | {{ version }} |
| **Generated** | {{ generation_date }} |
| **Status** | {{ status }} |
| **Project** | {{ project_name }} |

---

## 1. Introduction

This document specifies the Risk Analysis for {{ project_name }}. This specification is part of the Design History File (DHF) and provides comprehensive risk assessment and traceability for regulatory compliance per ISO 14971.

### 1.1 Purpose

This document provides a comprehensive list of all identified risks, their severity and probability assessments, risk control measures, and residual risk evaluations.

### 1.2 Scope

This specification covers all risks identified in the MedHarness system as of {{ generation_date }}.

---

## 2. Risk Analysis

{% for item in items %}
### {{ loop.index }}. {{ item.id }}: {{ item.title }}

<div class="risk-section" markdown="1">

**Status**: <span class="status-{{ item.status }}">{{ item.status | upper }}</span>  
{% if item.reviewer %}**Reviewer**: {{ item.reviewer }}  {% endif %}
{% if item.review_date %}**Review Date**: {{ item.review_date | format_date }}  {% endif %}

#### Hazard

{{ item.hazard }}

#### Cause

{{ item.cause }}

#### Effect

{{ item.effect }}

#### Pre-Mitigation Risk Assessment

| Metric | Value |
|--------|-------|
| **Severity** | {{ item.severity_pre }} |
| **Probability** | {{ item.probability_pre }} |
| **Risk Level** | {{ item.severity_pre }}-{{ item.probability_pre }} |

#### Post-Mitigation Risk Assessment

| Metric | Value |
|--------|-------|
| **Severity** | {{ item.severity_post }} |
| **Probability** | {{ item.probability_post }} |
| **Risk Level** | {{ item.severity_post }}-{{ item.probability_post }} |
| **Acceptability** | {{ item.risk_acceptability }} |

{% if item.risk_benefit %}
#### Risk-Benefit Analysis

{{ item.risk_benefit }}
{% endif %}

{% if item.mitigated_by %}
#### Risk Control Measures

{% for rcm in item.mitigated_by %}
- {{ rcm }}
{% endfor %}
{% endif %}

</div>

{% endfor %}

---

## 3. Risk Summary

### 3.1 Statistics

| Metric | Count |
|--------|-------|
| **Total Risks** | {{ items | length }} |
| **Approved** | {{ items | selectattr('status', 'equalto', 'approved') | list | length }} |
| **Under Review** | {{ items | selectattr('status', 'equalto', 'under_review') | list | length }} |
| **Draft** | {{ items | selectattr('status', 'equalto', 'draft') | list | length }} |

### 3.2 Risk Acceptability

| Acceptability | Count |
|---------------|-------|
| **Acceptable** | {{ items | selectattr('risk_acceptability', 'equalto', 'Acceptable') | list | length }} |
| **ALARP** | {{ items | selectattr('risk_acceptability', 'equalto', 'ALARP') | list | length }} |
| **Unacceptable** | {{ items | selectattr('risk_acceptability', 'equalto', 'Unacceptable') | list | length }} |

---

## 4. Document Control

**Document Owner**: Risk Management  
**Last Updated**: {{ generation_date }}  
**Next Review**: TBD

---

*This document was automatically generated by MedHarness in accordance with ISO 14971.*
