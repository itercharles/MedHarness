# {{ doc_type_name }} Specification

---

**Document Metadata**

| Field | Value |
|-------|-------|
| **Document ID** | {{ doc_type_code }}-SPEC |
| **Version** | {{ version }} |
| **Generated** | {{ generation_date }} |
| **Status** | {{ status }} |
| **Project** | {{ project_name }} |

---

## 1. Introduction

This document specifies the {{ doc_type_name }} for {{ project_name }}. This specification is part of the Design History File (DHF) and provides traceability for regulatory compliance.

### 1.1 Purpose

This document provides a comprehensive list of all {{ doc_type_name }}s, including their current status, content, and traceability links to related items.

### 1.2 Scope

This specification covers all {{ doc_type_name }}s defined in the MedHarness system as of {{ generation_date }}.

---

## 2. Requirements

{% for item in items %}
### {{ loop.index }}. {{ item.id }}: {{ item.title }}

<div class="requirement-section" markdown="1">

**Status**: <span class="status-{{ item.status }}">{{ item.status | upper }}</span>  
{% if item.reviewer %}**Reviewer**: {{ item.reviewer }}  {% endif %}
{% if item.review_date %}**Review Date**: {{ item.review_date | format_date }}  {% endif %}
{% if item.category %}**Category**: {{ item.category }}  {% endif %}
{% if item.verification_method %}**Verification Method**: {{ item.verification_method }}  {% endif %}

#### Description

{{ item.content }}

{% if item.links %}
#### Linked Items

{% for link in item.links %}
- {{ link }}
{% endfor %}
{% endif %}

{% if item.verification_status %}
#### Verification Status

**Status**: {{ item.verification_status }}
{% endif %}

</div>

{% endfor %}

---

## 3. Summary

### 3.1 Statistics

| Metric | Count |
|--------|-------|
| **Total Requirements** | {{ items | length }} |
| **Approved** | {{ items | selectattr('status', 'equalto', 'approved') | list | length }} |
| **Draft** | {{ items | selectattr('status', 'equalto', 'draft') | list | length }} |
| **Retired** | {{ items | selectattr('status', 'equalto', 'retired') | list | length }} |

### 3.2 Approval Status

{% set approved_count = items | selectattr('status', 'equalto', 'approved') | list | length %}
{% set total_count = items | length %}
{% if total_count > 0 %}
**Approval Rate**: {{ (approved_count / total_count * 100) | round(1) }}% ({{ approved_count }}/{{ total_count }})
{% endif %}

---

## 4. Document Control

**Document Owner**: Quality Assurance  
**Last Updated**: {{ generation_date }}  
**Next Review**: TBD

---

*This document was automatically generated by MedHarness.*

