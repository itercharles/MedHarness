# {{ doc_type_name }}

---

**Document Metadata**

| Field | Value |
|-------|-------|
| **Document ID** | {{ doc_type_code }}-SPEC |
| **Version** | {{ version }} |
| **Generated** | {{ generation_date }} |
| **Status** | {{ status }} |
| **Project** | {{ project_name }} |

---

## 1. Introduction

This document specifies the system architecture for {{ project_name }}. This specification is part of the Design History File (DHF) and provides comprehensive documentation of architectural decisions, design rationale, and technical implementation details.

### 1.1 Purpose

This document provides detailed architecture specifications including:
- System architecture and component design
- Data models and storage architecture
- Technology stack and implementation choices
- Design rationale and alternatives considered
- Interface definitions and integration points

### 1.2 Scope

This specification covers all architecture components defined in the CompliantFlow system as of {{ generation_date }}.

---

## 2. Architecture Components

{% for item in items %}
### {{ loop.index }}. {{ item.id }}: {{ item.title }}

<div class="architecture-section">

**Status**: <span class="status-{{ item.status }}">{{ item.status | upper }}</span>  
{% if item.component %}**Component**: {{ item.component }}  {% endif %}
{% if item.architecture_type %}**Architecture Type**: {{ item.architecture_type }}  {% endif %}

#### Overview

{{ item.content }}

{% if item.design_rationale %}
#### Design Rationale

{{ item.design_rationale }}
{% endif %}

{% if item.alternatives_considered %}
#### Alternatives Considered

{{ item.alternatives_considered }}
{% endif %}

{% if item.technology_stack %}
#### Technology Stack

{{ item.technology_stack }}
{% endif %}

{% if item.interfaces %}
#### Interfaces

{{ item.interfaces }}
{% endif %}

{% if item.links %}
#### Related Requirements

{% for link in item.links %}
- {{ link }}
{% endfor %}
{% endif %}

</div>

{% endfor %}

---

## 3. Summary

### 3.1 Architecture Statistics

| Metric | Count |
|--------|-------|
| **Total Architecture Components** | {{ items | length }} |
| **System Architecture** | {{ items | selectattr('architecture_type', 'equalto', 'system') | list | length }} |
| **Data Architecture** | {{ items | selectattr('architecture_type', 'equalto', 'data') | list | length }} |
| **UI Architecture** | {{ items | selectattr('architecture_type', 'equalto', 'ui') | list | length }} |
| **Integration Architecture** | {{ items | selectattr('architecture_type', 'equalto', 'integration') | list | length }} |

### 3.2 Component Status

| Status | Count |
|--------|-------|
| **Draft** | {{ items | selectattr('status', 'equalto', 'draft') | list | length }} |
| **Review** | {{ items | selectattr('status', 'equalto', 'review') | list | length }} |
| **Approved** | {{ items | selectattr('status', 'equalto', 'approved') | list | length }} |

### 3.3 Technology Overview

This architecture specification documents the following key technologies:
{% set tech_items = items | selectattr('technology_stack') | list %}
{% if tech_items %}
{% for item in tech_items %}
- **{{ item.title }}**: See {{ item.id }} for details
{% endfor %}
{% endif %}

---

## 4. Compliance and Traceability

### 4.1 Requirements Traceability

This architecture specification satisfies the following system requirements:
{% set all_links = [] %}
{% for item in items %}
  {% if item.links %}
    {% for link in item.links %}
      {% if link not in all_links %}
        {% set _ = all_links.append(link) %}
      {% endif %}
    {% endfor %}
  {% endif %}
{% endfor %}

{% if all_links %}
{% for link in all_links | sort %}
- {{ link }}
{% endfor %}
{% else %}
*No requirement links defined*
{% endif %}

### 4.2 IEC 62304 Compliance

This architecture documentation supports IEC 62304 compliance by providing:
- Clear system architecture definition (§5.3.1)
- Software item identification (§5.3.2)
- Interface specifications (§5.3.3)
- Design rationale and alternatives (§5.3.6)

---

## 5. Document Control

**Document Owner**: System Architecture Team  
**Last Updated**: {{ generation_date }}  
**Next Review**: TBD

---

*This document was automatically generated by CompliantFlow from architecture specification items.*
